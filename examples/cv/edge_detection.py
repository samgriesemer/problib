import numpy as np
from skimage.io import imread, imsave
from PIL import Image

from .. import processing

#threshold = 0.5
#img = np.float32(imread('~/Desktop/files/Griesemer, Sam.png'))
#edge = processing.edge_detection(np.float32(img[:,:,0])/255.0, threshold)

# show image
#Image.fromarray(edge*255.0).show()
